# IONOS Email Configuration
# Copy this file to .env and fill in your actual credentials

# IONOS SMTP Settings (Port 587 with STARTTLS)
SMTP_HOST=smtp.ionos.de
SMTP_PORT=587
SMTP_USER=support@studentenathome.de
SMTP_PASS=your-ionos-email-password

# Application Configuration
CANONICAL_BASE_URL=https://studentenathome.de
ADMIN_TOKEN=your-random-admin-token-min-64-chars-generate-with-openssl-rand-hex-32

# Security Configuration
JWT_SECRET=your-random-jwt-secret-min-64-chars-generate-with-openssl-rand-hex-64
NODE_ENV=production

# Database Configuration (Supabase or PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/database

# Notes:
# - Use your full IONOS email address as SMTP_USER
# - Use your regular email password for SMTP_PASS (no app password needed)
# - Port 587 uses STARTTLS encryption (as per your IONOS settings)
# - If smtp.ionos.de doesn't work, try: smtp.ionos.com or smtp.1und1.de
